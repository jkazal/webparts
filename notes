HelloWorldWebPart contient plusieurs instructions 'export', pour spécifier celle qu'on veut importer, on utilise { }. Pour l'instant, seul `IPointage` est disponible.

Le fichier MockHttpClient.ts exporte la classe MockHttpClient comme module par défaut, ce qui lui permet d'^etre importé par d'autres fichiers.

Feature Framwork Schema

https://docs.microsoft.com/fr-fr/sharepoint/dev/spfx/web-parts/get-started/provision-sp-assets-from-package
